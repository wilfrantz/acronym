
#include "../header/utilities.h"
#include "../header/functions.h"

#include <stdio.h> 
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include <unistd.h>

/* The edit function search the database for a given accronym
 * and open it in an editor to be edited. It receives the acronym
 * to be edited and the acronym file path from the routine. 
 * It checks if the file exit and has write permission to allow
 * the program to perform the edit task.
 * It returns a 0 value for succes and a non-zero for error.*/

int edit (char* arg, char* filePath){

	return (0);
}
